#include<stdio.h>
int main()
{
    int x,y;
    scanf("%d%d",&X,&Y);
    if(X>Y)
    {
        printf("x is greater than y");
    }
    else if(y>x) printf("y is greater than x");
    else print("both are equal");
    return 0;
}